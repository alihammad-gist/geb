import React from 'react';
import { Layout } from '../../layout';
import Head from '../../head';

export default () => {
    return (
        <Layout>
            <Head title="hello" />
            <h1>Wow</h1>
        </Layout>
    );
}